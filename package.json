{
  "name": "api-demo",
  "version": "1.0.0",
  "description": "Simple API to show documentation on Swagger",
  "main": "api/app.js",
  "scripts": {
    "start": "node api/bin/www",
    "dev": "DEBUG=app:* nodemon --require dotenv/config api/bin/www",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mateus Felix",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.6.9",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "express-swagger-generator": "^1.1.15",
    "nodemon": "^1.19.1"
  }
}
